// ゲーム設定
var CONFIG = {
    VERSION: 'v2026.01.12.1659',
    // ボードのグリッドサイズ（NxN）
    BOARD_SIZE: 5,
    // タッチ操作時のドラッグオフセット(px)
    // ブロックが指で隠れないよう、指の位置より上に表示
    TOUCH_DRAG_OFFSET_Y: 60,

    // ブロック消失アニメーション設定
    ANIMATION: {
        // アニメーション時間(ms)
        DURATION: 300,
        // 各ブロック間の遅延時間(ms)
        DELAY_PER_BLOCK: 50,
        // スケール倍率
        SCALE: 1.2,
        // 回転角度(度)
        ROTATION: 360
    },

    // デッキ用ブロック形状（テトリミノサイズまで）
    DECK_BLOCK_SHAPES: [
        // 1マス
        [[1]],

        // 2マス
        [[1, 1]],        // 1×2 横
        [[1], [1]],      // 2×1 縦

        // 3マス
        [[1, 1, 1]],     // 1×3 横
        [[1], [1], [1]], // 3×1 縦
        [[1, 0], [1, 1]], // L字型
        [[1, 1], [1, 0]], // L字型（鏡）
        [[0, 1], [1, 1]], // L字型（回転）

        // 2×2
        [[1, 1], [1, 1]],

        // L字型（4パターン）
        [[1, 0], [1, 0], [1, 1]],  // L
        [[1, 1, 1], [1, 0, 0]],    // L 90度回転
        [[1, 1], [0, 1], [0, 1]],  // L 180度回転
        [[0, 0, 1], [1, 1, 1]],    // L 270度回転

        // T字型（4パターン）
        [[1, 1, 1], [0, 1, 0]],    // T
        [[0, 1], [1, 1], [0, 1]],  // T 90度回転
        [[0, 1, 0], [1, 1, 1]],    // T 180度回転
        [[1, 0], [1, 1], [1, 0]],  // T 270度回転

        // S/Z字型（2パターン）
        [[1, 1, 0], [0, 1, 1]],    // S字
        [[0, 1, 1], [1, 1, 0]],    // Z字

        // I字型（1×4, 4×1）
        [[1, 1, 1, 1]],            // 1×4 横
        [[1], [1], [1], [1]]       // 4×1 縦
    ]
};
