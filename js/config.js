// ゲーム設定
var CONFIG = {
    VERSION: 'v2026.01.08.1816',
    // タッチ操作時のドラッグオフセット(px)
    // ブロックが指で隠れないよう、指の位置より上に表示
    TOUCH_DRAG_OFFSET_Y: 60,

    // ブロック消失アニメーション設定
    ANIMATION: {
        // アニメーション時間(ms)
        DURATION: 300,
        // 各ブロック間の遅延時間(ms)
        DELAY_PER_BLOCK: 50,
        // スケール倍率
        SCALE: 1.2,
        // 回転角度(度)
        ROTATION: 360
    }
};
